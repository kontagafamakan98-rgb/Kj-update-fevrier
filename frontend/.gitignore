# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build
# NOTE : la fonction Vercel api/og-jobs/[id].js (générée à CHAQUE build par
# le plugin generate-og-jobs-function de vite.config.js) est VOLONTAIREMENT
# COMMITTÉE (non gitignorée) : Vercel ne déploie pas les fonctions émises
# hors de l'outputDirectory pendant le build — le fichier doit exister dans
# le repo au moment du clone pour être collecté comme serverless function
# (dossier api/). Le HTML embarqué change à chaque build (assets hashés) :
# après un build local, committer la régénération si le fichier est modifié.

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
